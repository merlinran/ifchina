#project-list
  .row#headline
    .col-md-12
      %h1 我们的项目
      {% for cat in contents.projects.group_by_category %}
      .row
        {% for project in cat.entries %}
        .col-md-4
          %a(href='/projects/{{project._permalink}}' title='{{ project.description }}')
            {{project.photo.url | image_tag }}
            %h1 {{ project.name }}
        {% endfor %}
      {% endfor %}
